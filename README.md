# AF3_environment_on_dgx_spark
The current worked environment for install AF 3 on dgx-spark
# How to use
You can use af3_environment.yaml to create a conda environment, then use pip to insall require python package by requirements.txt, and replace pyproject.toml in the orignial af3 git repo, then install, it should work
